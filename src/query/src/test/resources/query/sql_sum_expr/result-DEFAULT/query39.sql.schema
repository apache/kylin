`count(1)` BIGINT NOT NULL,`sum(CASE WHEN (LSTG_FORMAT_NAME = ABIN) THEN PRICE ELSE 2 END)` DECIMAL(29,4),`count(DISTINCT SELLER_ID)` BIGINT NOT NULL,`count(DISTINCT ACCOUNT_ID)` BIGINT NOT NULL,`sum(ACCOUNT_ID)` BIGINT,`ACCOUNT_COUNTRY` STRING,`LSTG_FORMAT_NAME` STRING