

--disable reason: https://github.com/Kyligence/KAP/issues/14426
select count(distinct date_format(TEST_TIME_ENC,'y')),
       count(distinct date_format(TEST_TIME_ENC,'Y')),
       count(distinct date_format(TEST_TIME_ENC,'y')),
       count(distinct date_format(TEST_TIME_ENC,'M')),
       count(distinct date_format(TEST_TIME_ENC,'M')),
       count(distinct date_format(TEST_TIME_ENC,'d')),
       count(distinct date_format(TEST_TIME_ENC,'D')),
       count(distinct date_format(TEST_TIME_ENC,'d')),
       count(distinct date_format(TEST_TIME_ENC,'h')),
       count(distinct date_format(TEST_TIME_ENC,'H')),
       count(distinct date_format(TEST_TIME_ENC,'h')),
       count(distinct date_format(TEST_TIME_ENC,'m')),
       count(distinct date_format(TEST_TIME_ENC,'m')),
       count(distinct date_format(TEST_TIME_ENC,'m')),
       count(distinct date_format(TEST_TIME_ENC,'s')),
       count(distinct date_format(TEST_TIME_ENC,'s')),
       count(distinct date_format(TEST_TIME_ENC,'s'))
from TEST_ORDER